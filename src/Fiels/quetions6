6-_____($var) : renvoie vrai si la variable est vide
12-Quelle fonction est utilisée pour supprimer les espaces au début d’une chaîne?_____()
18-L’identifiant ‘%i’ est également utilisé pour _____?
24-<___> : crée une zone de saisie
30-______($tab) : trie alphanumérique les éléments du tableau
36-______($var) : détruit une variable
-_____($var) : renvoie vrai si la variable est vide       
-Quelle fonction est utilisée pour supprimer les espaces au début d’une chaîne?_____()  
-L’identifiant ‘%i’ est également utilisé pour _____?  
-<___> : crée une zone de saisie    
-______($tab) : trie alphanumérique les éléments du tableau             
-______($var) : détruit une variable                        
-_____($var) : renvoie vrai si la variable est vide       
-Quelle fonction est utilisée pour supprimer les espaces au début d’une chaîne?_____()  
-L’identifiant ‘%i’ est également utilisé pour _____?  
-<___> : crée une zone de saisie    
-______($tab) : trie alphanumérique les éléments du tableau             
-______($var) : détruit une variable                        
-_____($var) : renvoie vrai si la variable est vide       
-Quelle fonction est utilisée pour supprimer les espaces au début d’une chaîne?_____()  
-L’identifiant ‘%i’ est également utilisé pour _____?  
-<___> : crée une zone de saisie    
-______($tab) : trie alphanumérique les éléments du tableau             
-______($var) : détruit une variable                        
-_____($var) : renvoie vrai si la variable est vide       
-Quelle fonction est utilisée pour supprimer les espaces au début d’une chaîne?_____()  
-L’identifiant ‘%i’ est également utilisé pour _____?  
-<___> : crée une zone de saisie    
-______($tab) : trie alphanumérique les éléments du tableau             
-______($var) : détruit une variable                        
-_____($var) : renvoie vrai si la variable est vide       
-Quelle fonction est utilisée pour supprimer les espaces au début d’une chaîne?_____()  
-L’identifiant ‘%i’ est également utilisé pour _____?  
-<___> : crée une zone de saisie    
-______($tab) : trie alphanumérique les éléments du tableau             
-______($var) : détruit une variable                        