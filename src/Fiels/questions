-Quelle est l'extension d'un fichier de fichier de données JSON? 
-Quel composant est utilisé pour compiler, déboguer et exécuter un programme java?    
-Quel composant est responsable de la conversion du byte-codes en code spécifique à la machine? 
-Quel composant est responsable de l’exécution du programme Java?   
-Quelle fonction pour comparer deux chaines de caractères?_____()  
-Quel est le type de données pour le nombre 8.2917 ? double
-Quelle est l'extension d'un fichier de fichier de données JSON? 
-Quel composant est utilisé pour compiler, déboguer et exécuter un programme java?    
-Quel composant est responsable de la conversion du byte-codes en code spécifique à la machine?  
-Quel composant est responsable de l’exécution du programme Java?   
-Quelle fonction pour comparer deux chaines de caractères?_____()  
-Quel est le type de données pour le nombre 8.2917 ? 
-Quelle est l'extension d'un fichier de fichier de données JSON? 
-Quel composant est utilisé pour compiler, déboguer et exécuter un programme java?    
-Quel composant est responsable de la conversion du byte-codes en code spécifique à la machine?  
-Quel composant est responsable de l’exécution du programme Java?   
-Quelle fonction pour comparer deux chaines de caractères?_____()  
-Quel est le type de données pour le nombre 8.2917 ? double
-Quelle est l'extension d'un fichier de fichier de données JSON? 
-Quel composant est utilisé pour compiler, déboguer et exécuter un programme java?    
-Quel composant est responsable de la conversion du byte-codes en code spécifique à la machine?  
-Quel composant est responsable de l’exécution du programme Java?   
-Quelle fonction pour comparer deux chaines de caractères?_____()  
-Quel est le type de données pour le nombre 8.2917 ? 
-Quelle est l'extension d'un fichier de fichier de données JSON? 
-Quel composant est utilisé pour compiler, déboguer et exécuter un programme java?    
-Quel composant est responsable de la conversion du byte-codes en code spécifique à la machine?  
-Quel composant est responsable de l’exécution du programme Java?   
-Quelle fonction pour comparer deux chaines de caractères?_____()  
-Quel est le type de données pour le nombre 8.2917 ? 
-Quelle est l'extension d'un fichier de fichier de données JSON? 
-Quel composant est utilisé pour compiler, déboguer et exécuter un programme java?    
-Quel composant est responsable de la conversion du byte-codes en code spécifique à la machine?  
-Quel composant est responsable de l’exécution du programme Java?   
-Quelle fonction pour comparer deux chaines de caractères?_____()  
-Quel est le type de données pour le nombre 8.2917 ? 